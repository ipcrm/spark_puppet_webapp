ipcrm/sparkjava-puppet-webapp-build:
  Env:
    - PORT: "9999"
  Build:
    - mvn clean package -s settings.xml -U
  PkgInclude:
    - 'target/puppet-webapp-*.jar'
  Exec:
    - powershell -command "java -DappPort=%PORT% -jar %cd%/target/puppet-webapp-1.2.jar"


